import { getTrainingDataProviderStatus } from "../src/providers/training-data-service.js";const mark=(ok:boolean)=>ok?"OK":"--";console.log("COROS Workout MCP Doctor\n");console.log(`${mark(Number(process.versions.node.split('.')[0])>=22)} Node.js ${process.versions.node}`);for(const p of await getTrainingDataProviderStatus()) console.log(`${mark(p.available)} ${p.displayName} [${p.id}] ${p.capabilities.join(', ')}`);console.log(`\n${mark(Boolean(process.env.COROS_EMAIL))} COROS_EMAIL`);console.log(`${mark(Boolean(process.env.COROS_PASSWORD))} COROS_PASSWORD`);console.log(`${mark(process.env.COROS_ALLOW_UNOFFICIAL_MOBILE_LOGIN!=='true')} unofficial mobile login disabled`);console.log("\nSecrets are never printed by this command.");
